name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - run: npx playwright install --with-deps chromium

      - name: Generate Linux visual snapshots if missing
        run: |
          if ! find tests/playwright/app.spec.ts-snapshots -name "*-linux.png" -exit 0 >/dev/null 2>&1; then
            npx playwright test --grep @visual --update-snapshots
          fi

      - run: npm run ci

      - name: Upload failure artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ci-artifacts
          if-no-files-found: ignore
          path: |
            coverage/**
            playwright-report/**
            test-results/**
